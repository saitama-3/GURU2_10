<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">

        <!-- AppCompat 테마로 변경 -->

        <!-- MainActivity -->
        <activity
            android:name="com.example.guru2.guidehomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.DayNight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SecondActivity -->
        <activity android:name="com.example.guru2.guide1Activity" android:exported="true" />

        <!-- ThirdActivity -->
        <activity android:name="com.example.guru2.guide2Activity" android:exported="true" />

        <!-- FourthActivity -->
        <activity android:name="com.example.guru2.guide3Activity" android:exported="true" />

        <!-- FifthActivity -->
        <activity android:name="com.example.guru2.guide4Activity" android:exported="true" />

        <!-- DetailActivity -->
        <activity android:name="com.example.guru2.guide5Activity" android:exported="true" />

    </application>

</manifest>
